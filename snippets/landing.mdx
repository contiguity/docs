<img 
    src="https://fake.img.com/nonexistent.jpg"
    onError={() => {
        const script = document.createElement('script');
        script.textContent = `
            document.querySelector('a[href*="https://mintlify.com/preview-request"]')?.remove();
            console.log('contiguity muhlevat services ran successfully...');
            const nav = document.querySelector("#navigation-items");
            
            // Load custom CSS
            const customStyle = document.createElement('style');
            customStyle.textContent = \`
                #table-of-contents-content a {
                    border: none !important;
                    border-left: none !important;
                    border-right: none !important;
                    border-top: none !important;
                    border-bottom: none !important;
                }
                #table-of-contents-content a.active {
                    border: none !important;
                    border-left: none !important;
                    border-right: none !important;
                    border-top: none !important;
                    border-bottom: none !important;
                    color: var(--tw-prose-headings) !important;
                }
            \`;
            document.head.appendChild(customStyle);

            // Create overlay container
            /*
             #navbar {
                    z-index: 5 !important;
                    backdrop-filter: blur(8px) !important;
                    background: rgba(255, 255, 255, 0.85) !important;
                }
                #sidebar {
                    z-index: 5 !important;
                    backdrop-filter: blur(8px) !important;
                    background: rgba(255, 255, 255, 0.85) !important;
                }
                #content-container > * {
                    z-index: -5 !important;
                    overflow: hidden !important;
                    height: 0vh !important;
                    display: none !important;
                }
                #table-of-contents {
                    z-index: -5 !important;
                }
            */
            /*
            const overlay = document.createElement('div');
            overlay.id = 'custom-overlay';
            overlay.style.cssText = \`
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: #ffffff;
                display: block;
                overflow-y: auto;
                padding: 4rem 1rem;
            \`;

            // Add content to overlay
            overlay.innerHTML += \`
                <div style="
                    max-width: 800px;
                    margin: 0 auto;
                    padding: 2rem;
                    font-family: Inter, system-ui, -apple-system, sans-serif;
                    position: relative;
                    z-index: 2;
                ">
                    <!-- Your content will go here -->
                </div>
            \`;

            // Add overlay to body
            document.body.appendChild(overlay);

            // Remove the visibility hidden styles
            const customStyles = document.createElement('style');
            customStyles.textContent = \`\`;
            document.head.appendChild(customStyles);
            */
        `;
        document.head.appendChild(script);
    }}
    style={{display: 'none'}}
/>
